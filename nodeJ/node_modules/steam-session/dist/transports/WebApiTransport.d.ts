import { HttpClient } from '@doctormckay/stdlib/http';
import ITransport, { ApiRequest, ApiResponse } from './ITransport';
export interface WebApiTransportOptions {
    httpProxy?: string;
    socksProxy?: string;
}
export default class WebApiTransport implements ITransport {
    _client: HttpClient;
    constructor(client: HttpClient);
    sendRequest(request: ApiRequest): Promise<ApiResponse>;
    close(): void;
}
//# sourceMappingURL=WebApiTransport.d.ts.map