/// <reference types="node" />
/// <reference types="node" />
import { EventEmitter } from 'events';
import { HttpClientOptions, HttpRequestOptions, HttpResponse, MultipartFormObject } from './types';
import CookieJar from './CookieJar';
export default class HttpClient extends EventEmitter {
    #private;
    cookieJar?: CookieJar;
    userAgent?: string;
    constructor(options?: HttpClientOptions);
    request(options: HttpRequestOptions): Promise<HttpResponse>;
    static simpleObjectToMultipartForm(obj: {
        [name: string]: string | Buffer;
    }): {
        [name: string]: MultipartFormObject;
    };
}
export declare function preProcessOptions(options: HttpRequestOptions): HttpRequestOptions;
//# sourceMappingURL=HttpClient.d.ts.map